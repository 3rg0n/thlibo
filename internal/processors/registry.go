// Package processors loads processor descriptors from ~/.thlibo/processors/
// (and built-ins embedded in the binary), builds a registry, and dispatches
// input to script or prompt processors.
package processors
