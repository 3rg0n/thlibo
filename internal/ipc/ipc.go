// Package ipc implements the daemon's socket surface: platform-specific
// socket/pipe creation with correct ownership and permissions, and the
// newline-delimited JSON request/response protocol defined in the spec.
package ipc
